// O programa calcula e apresenta os quadrados dos números inteiros de 15 a 200.

for (let contador = 15; contador <= 200; contador++) {
    let quadrado = contador * contador;
    console.log(`O quadrado de ${contador} é: ${quadrado}`); 
}