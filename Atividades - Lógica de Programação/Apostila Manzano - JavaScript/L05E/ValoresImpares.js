// O programa apresenta todos os valores inteiros ímpares na faixa de 0 a 20.

for (let contador = 0; contador <= 20; contador++) {
    if (contador % 2 !== 0) {
        console.log(`Os valores ímpares são: ${contador}`);
    }
}
